---
- name: Install vim-enhanced latest
  when: version == 'latest'
  become: yes
  apt:
    update_cache: yes
    name: vim
    state: latest

- name: Install vim-enhanced specific version
  when: version != 'latest'
  become: yes
  apt:
    update_cache: yes
    name: vim={{ version }}
    state: present